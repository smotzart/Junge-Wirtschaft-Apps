<GridLayout rows="*" xmlns:Card="nativescript-cardview" xmlns:IC="nativescript-web-image-cache">
  <GridLayout row="0" rows="180, auto" cssClass="empty-list" horizontalAlignment="center" visibility="{{ (events_count == 0 && !isLoading) ? 'visible' : 'collapsed' }}">
    <StackLayout row="0" verticalAlignment="center" horizontalAlignment="center">
      <image src="~/images/empty-man.png" stretch="none" />
    </StackLayout>
    <Label row="1" text="Leider gibt es in dieser Kategorie keine Veranstaltungen" textWrap="true" />
  </GridLayout>
  <ListView items="{{ events }}" row="0" visibility="{{ (events_count > 0 && !isLoading) ? 'visible' : 'collapsed' }}">
    <ListView.itemTemplate>
      <StackLayout>
        <StackLayout marginTop="20" verticalAlignment="center" height="40" cssClass="card-separate-head" paddingLeft="10">
          <label text="{{ month_name }}" />
        </StackLayout>
        <Repeater items="{{ list }}">
          <Repeater.itemsLayout>
            <WrapLayout />
          </Repeater.itemsLayout>
          <Repeater.itemTemplate>
            <Card:CardView cssClass="card card-wide card-event" elevation="7" radius="1" tap="{{ goToEventView }}">
              <GridLayout rows="124, auto" columns="*">
                <StackLayout row="0" cssClass="bg-placeholder">
                  <IC:WebImage height="124" stretch="aspectFill" src="{{ event.banner_url }}"></IC:WebImage> 
                </StackLayout>
                <GridLayout row="1" rows="20, auto, auto, auto" columns="*, *" cssClass="inner">
                  <StackLayout cssClass="card-attr" row="0" colSpan="2" verticalAlignment="center">
                    <label text="{{ event_start + ' - ' + event_end }}" textWrap="true" />
                  </StackLayout>
                  <StackLayout row="1" colSpan="2" verticalAlignment="center">
                    <label text="{{ event.name }}" cssClass="h2" textWrap="false" />
                    <label text="{{ event.description | textTruncate(100) }}" cssClass="card-subhead" textWrap="true" visibility="{{ event.description ? 'visible' : 'collapsed' }}"  />
                  </StackLayout>
                  <StackLayout row="2" colSpan="2" cssClass="card-hr" visibility="{{ (event.place || event.category) ? 'visible' : 'collapsed' }}" />
                  <StackLayout cssClass="card-attr" height="20" row="4" col="0" verticalAlignment="center" orientation="horizontal" visibility="{{ event.place ? 'visible' : 'collapsed' }}">
                    <label text="&#xf041;" cssClass="fa" />
                    <label text="{{ event.place }}" />
                  </StackLayout>
                  <StackLayout cssClass="card-attr" height="20" row="3" col="1" verticalAlignment="center" orientation="horizontal" visibility="{{ event.category ? 'visible' : 'collapsed' }}">
                    <label text="&#xf02b;" cssClass="fa" />
                    <label text="{{ event.category }}" />
                  </StackLayout>
                </GridLayout>
              </GridLayout>
            </Card:CardView>
          </Repeater.itemTemplate>
        </Repeater>
      </StackLayout>
    </ListView.itemTemplate>
  </ListView>
  <ActivityIndicator busy="{{ isLoading }}" />
</GridLayout>
